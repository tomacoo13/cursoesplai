// 3.Copy Recorrer la siguiente lista con un bucle imprimiendo el doble de cada número:

const numbers = [1, 9, 3, 8, 5, 7]

for (let i = 0; i < numbers.length; i++) {

    console.log(numbers[i] * 2)
}
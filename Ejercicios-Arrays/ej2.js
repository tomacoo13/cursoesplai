// 2.Copy Crea una función toArray que reciba dos valores y devuelva un array con estos dos valores.

// Por ejemplo, la llamada: toArray(5,9);

const toArray = (valor1, valor2) => {
    newArray = [valor1, valor2]
    return newArray

}

console.log(toArray(5, 8))